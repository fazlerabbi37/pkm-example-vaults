# Zettelkasten Vault
Flat pile of atomic notes. IDs are timestamps and never change.
Folders are minimal; LINKS do all the organizing.
Rules: one idea per note, always link to at least one other note,
never rename the ID part of a filename.
