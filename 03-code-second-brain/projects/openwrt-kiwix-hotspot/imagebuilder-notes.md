# ImageBuilder notes (distilled)
Layer 1 — captured text:
ImageBuilder assembles pre-compiled packages into a firmware image.
It requires roughly 2GB of RAM on the build host and a case-sensitive fs.

Layer 2 — bolded on second read:
**assembles pre-compiled packages** ... **2GB of RAM on the build host**

Layer 3 — executive summary (my words):
It's an assembler not a compiler; my laptop is fine, the router never
builds anything.
